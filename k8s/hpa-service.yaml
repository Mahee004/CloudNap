apiVersion: v1
kind: Service

metadata:
  name: hello-api-hpa-service
  namespace: default

spec:
  selector:
    app: hello-api-hpa

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: NodePort